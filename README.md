# 中文命令行

## 用法:
1. 安装Python 3.8+
2. 运行
    Windows用 `python 主程序.py`
    其他系统用 `python3 主程序.py`
3. 输入命令 (不知道如何使用就输入 `帮助` 查看帮助)

目前只有几个命令，欢迎大家制作新的命令并提交Pull Request。

## ⚠️警告
- 我的世界需要安装启动器(如 PCL 或 HMCL), 导出启动脚本并修改`命令/我的世界.py`中的`启动脚本路径`为实际路径 (支持 .sh, .bat 等可执行文件)
